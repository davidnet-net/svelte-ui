replicaCount: 1

image:
  repository: ghcr.io/davidnet-net/svelte-ui
  pullPolicy: Always
  tag: "latest"

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  host: design.davidnet.net
  ingressClass: traefik
